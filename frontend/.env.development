# ===== TẠM THỜI DÙNG NODE.JS CHO TẤT CẢ (Bỏ Drupal) =====
# Vì DevTunnel không hoạt động, tạm thời dùng Node.js backend cho cả Auth
# Sửa lại thành đường dẫn Proxy (không còn http://localhost:8000 nữa)
VITE_DRUPAL_API=/api/drupal

# Tương tự với Node
VITE_NODE_API=/api/node
VITE_SOCKET_URL=http://localhost:5001